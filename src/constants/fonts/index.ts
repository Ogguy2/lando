import {Playfair, Playfair_Display} from "next/font/google";
import {Inter} from "next/font/google"
export const fairPlayFont = Playfair_Display({
    subsets: ["latin"],
})

export const interFont = Inter({
    subsets: ["latin"],
})
